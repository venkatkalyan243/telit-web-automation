<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
  <Properties>
    <Property name="LOG_DIR">target/logs</Property>
    <Property name="ARCHIVE_DIR">${LOG_DIR}/archive</Property>
  </Properties>

  <Appenders>
    <!-- Console Appender -->
    <Console name="ConsoleAppender" target="SYSTEM_OUT">
      <PatternLayout pattern="%d{yyyy-MMM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n%throwable"/>
    </Console>

    <!-- Rolling File Appender -->
    <RollingFile name="FileAppender"
                 fileName="${LOG_DIR}/execution.log"
                 filePattern="${ARCHIVE_DIR}/execution_%d{yyyy-MMM-dd_HH-mm}.log">
      <PatternLayout>
        <Pattern>%d{yyyy-MMM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n</Pattern>
      </PatternLayout>
      <Policies>
        <!-- Creates a new file every time you start a test suite -->
        <OnStartupTriggeringPolicy/>
        <!-- Or if the file reaches 10MB -->
        <SizeBasedTriggeringPolicy size="10 MB"/>
      </Policies>
    </RollingFile>
  </Appenders>

  <Loggers>
    <Root level="INFO">
      <AppenderRef ref="ConsoleAppender"/>
      <AppenderRef ref="FileAppender"/>
    </Root>
  </Loggers>
</Configuration>
